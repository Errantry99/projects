{
  "name": "@dcx/hitl",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local HITL review page (reads/writes the SQLite journal only)",
  "license": "Apache-2.0",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "vitest run",
    "build": "tsc -b tsconfig.build.json"
  },
  "dependencies": {
    "@dcx/core": "*",
    "@dcx/store": "*"
  }
}
