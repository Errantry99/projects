{
  "name": "@dcx/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared types, JCS hashing, cache key, DDL and migrations",
  "license": "Apache-2.0",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p .",
    "test": "vitest run",
    "build": "tsc -b tsconfig.build.json",
    "vectors": "tsx scripts/write-vectors.ts"
  },
  "dependencies": {
    "@duckdb/node-api": "^1.5.5-r.5",
    "better-sqlite3": "^13.0.3"
  }
}
