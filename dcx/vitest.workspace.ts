// Vitest removed standalone workspace files in v4; the project list lives here so the spec's
// file name keeps its meaning, and `vitest.config.ts` passes it to `test.projects`.
export const projects = ["packages/*", "projects/*"];
